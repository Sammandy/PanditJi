import React from 'react';
import { Container, Typography } from '@mui/material';

function Account() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Account
      </Typography>
      <Typography variant="body1">This is the Account page.</Typography>
    </Container>
  );
}

export default Account;
